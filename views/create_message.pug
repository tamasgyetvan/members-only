extends layout 

block content 
    h1 Create message
    form#messageForm(method="POST" action="")
        label(for="title") Title:
        input#title(type="text", name="title" value=((message != null) ? message.title : ""))

        label(for="message") Message:
        textarea#message(name="message" cols="20" rows="5" value=((message != null) ? message.message : ""))

        button#submitBtn Submit
    
    if errors
        ul
            for error in errors
                li!= error.msg

    
    a(href="/home") Go back