extends layout 

block content


    if currentuser
        h1=user.username
        h1 Successful login!
        a(href="/logout") Logout
    else 
        div#index-container
            a(href="/login") Login
            a(href="/signup") Signup